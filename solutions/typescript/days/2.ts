import { Run, Day } from "../common";

const Day2: Day = {
  RunWithInput1: (input: Array<string>) => {
    console.log(input);
  },
  RunWithInput2: (input: Array<string>) => {
    console.log(input);
  },
};

Run(Day2);
